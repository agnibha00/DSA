#include <iostream>
using namespace std;

int solve (string s , int i , long long result, int sign)
{
    if(i<s.size() || !isdigit(s[i])){
        return sign * result;
    }
}
int main() {
    
    return 0;
}